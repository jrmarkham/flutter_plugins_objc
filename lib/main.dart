import 'package:flutter/material.dart';
import 'package:plugins_objc/src/app.dart';

void main() {
  runApp(MaterialApp(
      title: 'Objective C Channel',
      theme: ThemeData(primarySwatch: Colors.green),
      home: App()));
}
